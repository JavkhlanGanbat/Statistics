% Safe macro definitions (only define if not already defined)
\providecommand{\myterm}{\textbf{Important Term}}

% Simple boxed text macro (uses fbox; for richer styling load tcolorbox instead)
\providecommand{\mybox}[1]{\fbox{\strut #1}}

% If you previously loaded a different version elsewhere, remove that duplicate.