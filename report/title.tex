% Custom macros and packages for the report
\usepackage{tcolorbox}
\usepackage{xcolor}

% Safe macro definitions (only define if not already defined)
\providecommand{\myterm}[1]{\textbf{#1}}
\providecommand{\mybox}[1]{\fbox{\strut #1}}